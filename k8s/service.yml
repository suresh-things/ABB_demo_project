apiVersion: v1
kind: Service
metadata:
  name: my-nodeapp
  namespace: my-nodeapp
spec:
  selector:
    app: my-nodeapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
